#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a, b;
    printf("Enter the number a: ");
    scanf("%d", &a);
    printf("Enter the number b: ");
    scanf("%d", &b);
    printf("Your added sum is %d", a+b);
    return 0;
}
